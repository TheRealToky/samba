# Single consolidated backend image, run in several roles (api / worker /
# inference / training) via different commands. Heavy deep-learning deps
# (torch) are intentionally omitted — Phase 3 baselines are classical; the
# Earth Engine client is an optional lazy import (live satellite mode only).
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
GeoAlchemy2==0.16.0
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
argon2-cffi==23.1.0
PyJWT==2.10.1
email-validator==2.2.0
shapely==2.0.6
httpx==0.28.1
redis==5.2.1
rq==2.1.0
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
scikit-learn==1.6.0
statsmodels==0.14.4
reportlab==4.2.5
boto3==1.35.90
pytest==8.3.4
